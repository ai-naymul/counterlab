# Copy to .env and fill in. NEVER commit .env.
# All of these are OPTIONAL - CounterLab runs in rules-only mode without them.

GEMINI_API_KEY=
COUNTERLAB_CONTACT_EMAIL=

# Optional: Render Workflows (Deep Audit)
RENDER_API_KEY=
COUNTERLAB_WORKFLOW_SLUG=
